import { WineImagePipe } from './wine-image.pipe';

describe('WineImagePipe', () => {
  it('create an instance', () => {
    const pipe = new WineImagePipe();
    expect(pipe).toBeTruthy();
  });
});
